# macOS system files
.DS_Store

# Unpacked and archive directories (ignore all contents)
temp_unzip/
archive/
ldmmifpegigmeammaeckplhnjbbpccmm.zip

# Node modules (if ever reintroduced)
node_modules/

.# Editor / workspace
*.code-workspace
.vscode/

# test artifacts / logs
tests/log/

# packaged artifacts
save-to-notion-reversed-*.tgz

.git.backup-*
# Backup directories created by external tooling — collapsed to a single glob
